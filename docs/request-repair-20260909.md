# 请求链路修复与验收（2026-09-09）

本次修复阶段回复解析丢失、协议错误误判完成、首次技能索引准备滞后，以及供应商地址/协议/凭据传递缺项。桌面“白球AI-测试-11”已加载修改后的 source-current，版本标识仍为 3.0.22。没有发布客户安装包。

## 修改范围

| 文件 | 修改 |
|---|---|
| `services/hms-progress.js` | 接收 progress JSON 时，不再把正文中的反引号、动作围栏或动作形态文字按外围动作处理；真实阶段正文完整保留。无公开标记的推理通道不再进入公开进度、答案或协议错误统计；正常工具事件及明确公开的消息继续处理。 |
| `services/hermes-config-service.js` | 原生供应商也明确配置 API 模式；将已配置地址写入对应的原生地址环境变量，避免 GLM 等再次探测/使用错误区域地址；补齐 Kimi 中国区和 TokenHub 的凭据变量。自定义兼容地址继续使用独立命名配置。 |
| `services/baiqiu_acp_entry.py` | 创建匹配当前供应商的 Agent 时明确传入地址和协议。新建/加载/恢复会话完成工具注册后，准备原生技能索引，使用相同工作目录、工具集及平台条件，复用原生缓存，不发送模型请求。准备异常保留正常首次构建路径。 |
| `main.js` | 两条调用路径将协议错误标为失败；最终交付分支不再把残留字符或空回复标为完成。保留已产生的回答和执行证据，不自动重试已执行工具。 |

测试修改：`test/hms-progress.test.js`、`test/hms-channel-isolation.test.js`、`test/test_tool_generation_bridge.py`；新增 `test/request-repair.test.js`。没有修改字体、小剧场、页面布局、颜色或工具执行实现，没有修改已安装 Hermes 源文件。

## 回归验证

- 132 项针对性 Node 检查通过，覆盖协议分块、行内代码/动作形态正文、真假公开通道、单字合法回答、错误完成判定、供应商配置、会话预热/恢复、超时分类及回放边界。
- 15 项 Python 桥接检查通过，覆盖原生缓存准备、上下文清理、地址/协议传入及原有供应商桥接行为。
- 使用上轮两次真实任务的原始 ACP 分块回放：修复前输出 0/4 段阶段结果且协议报错；修复后输出 4/4 段，正文逐字一致，无协议错误。
- 使用真实失败任务的公开记录执行当前完成分支：残留 `"}` 保持为失败，`publicFinalFound=false`，不再标为完成。这是源码分支回放，不是向桌面发送伪造事件。
- 扩展运行旧 `persisted-stream-replay-regression.test.js` 另有 4 项失败，错误均为测试 VM 中 `persistedReplay is not defined`。该测试只读未修改的 renderer 文件并自行建立 VM，本轮未修改该测试或 renderer，未把这四项记作通过；详情保留在扩展日志。

## 供应商适配范围

14 种预设/自定义配置使用离线占位凭据，实际调用已安装运行时的供应商解析函数；地址、凭据解析和协议检查均通过，模型网络请求为 0。覆盖 DeepSeek、OpenAI、Kimi、Anthropic、Qwen、百度、GLM、豆包、混元、MiniMax、StepFun、小米、Ollama 配置及自定义兼容配置。

这证明配置传递与运行时解析相符，不证明 14 家上游都稳定。真实网络验收只使用用户已配置凭据的官方 GLM 和一个第三方兼容入口。没有其他供应商正式密钥，未声称完成其真实服务验收。Ollama 的离线占位凭据检查也不代表本机服务实际运行。

官方 GLM 请求保留 15 个工具、`reasoning_effort=max`、`thinking.enabled`、`clear_thinking=false`、`tool_stream=true`。第三方仍保留现有适配字段及 15 个工具，其请求未携带原生 reasoning_effort；没有证据证明该别名实际支持官方的 max 参数，因此不把它算作“已验证最大推理”。

## 性能与真实任务结果

首次本地准备的无模型请求剖析：修改前 1,601.938 ms，修改后 164.306 ms；同进程重复分别为 15.187 ms、12.558 ms。之前约 1.39 秒的技能扫描已移到会话准备阶段。此为独立冷目录对照，不是总回复时间下降 90%，也不代表开软件无需等待准备。

真实隔离运行时验收：

| 场景 | 公开内容首个分块 | 首个映射执行事件 | 最终答案首字 | 总耗时 | 正确性 |
|---|---:|---:|---:|---:|---|
| GLM 新会话，两文件分阶段读取 | 22.957 s | 23.990 s | 41.934 s | 43.643 s | 两次真实读取，2 段阶段结果，差值 12，正文完整 |
| GLM 同会话追问 | 5.795 s | 无，模型未产生工具/进度事件 | 5.795 s | 6.730 s | 正确复述 17、29、12 |
| GLM 停止并重建运行时后恢复 | 6.960 s | 无，模型未产生工具/进度事件 | 6.960 s | 7.882 s | 原会话 ID 保持，正确复述 17、29、12，无重复读文件 |
| 第三方兼容入口，两文件分阶段读取 | 3.909 s | 4.845 s | 24.933 s | 25.558 s | 两次真实读取及一次计算，2 段阶段结果，差值 12，正文完整 |

新任务提交至首个 HTTP 分发：GLM 426 ms、第三方 583 ms；GLM 同会话追问 16 ms、恢复后 492 ms。隔离配置关闭了后台标题生成以避免混入测量，没有修改生产标题配置。

第三方最终答案只写差值，17 和 29 已在两段阶段结果中给出。只检查最终答案是否同时含三个数字会误报失败；结合真实读取证据、阶段正文和最终答案后，该任务正确。原始 `numericAnswerCorrect=false` 保留在记录中，完整判据结果另存于 replay-validation.json，未篡改原测量。

桌面端完整验收使用当前选中的官方 GLM，创建“请求修复验收 2026-09-09”独立会话，按正常 sendCurrentTask 路径执行真实读取：

- 提交前显式等待预热完成，耗时 4.707 秒；不能据此声称立即打开软件就提交也没有冷准备等待。
- 提交至首个真实公开执行事件：21.542 秒；最终答案首字：47.566 秒；完整任务：50.155 秒。
- 两次真实读取，库存 17 与 29，最终差值 12。模型自行产生 3 段阶段结果（第三段为差值比较），三段均保留；前端没有补齐固定阶段。
- 切入历史会话回放后，3 个阶段节点、最终答案及行内代码均存在。验收脚本最初使用错误 DOM 选择器，随后用实际 messageList 验证并把结果存入 rendered 字段；未用空选择器结果声称 UI 成功。
- 已切回原会话，当前无活动发送，输入框为空。旧 350 条消息的角色、正文及创建时间哈希均未变，所有 API Key 未变，仍选择 GLM / maximum。

## 剩余限制

速度没有达到“所有任务秒回”。GLM 隔离新任务首轮响应头等待约 4.63 秒，首个公开内容距请求发出约 22.54 秒；本地修复不能直接消除供应商响应和生成耗时。输入提示词、工具能力、推理强度均未为追求计时数字而削弱。

连接无响应、持续有流但长期无公开内容、流停滞与总时限已有独立判定，本轮通过既有定时器控制测试确认其边界，没有加长时限来掩盖等待，也没有新增自动重试。协议合法但语义上遗漏用户步骤，不可能仅凭 `end_turn` 通用判定已经完成；本轮修复的是已证明的协议错误/空交付误报，并未新增猜测式任务完成判断器。

桌面启动与验收前后的 settings 整体哈希不同；本轮没有保存各设置字段的原始快照，因此没有把所有设置声明为完全不变。已独立核对凭据、模型、推理选择及旧消息；隔离测试阶段 settings 整体哈希保持一致。

## 证据与回退

证据目录：`C:/Users/Lenovo/Desktop/白球AI-客户端/work/request-repair-20260909/`。

- `acceptance.json`：隔离真实请求、时间诊断及无模型请求剖析。
- `provider-contract.json`：14 个配置的运行时解析。
- `replay-validation.json`：真实分块修复前后对照、真实失败终态回放及完整任务判据。
- `desktop.json`：桌面发送、持久化、历史 DOM 核对与消息完整性。
- `focused-tests.log`：132/132；`node-tests.log`：包含旧回放 VM 失败的扩展记录。
- `integrity.json`：本次覆盖文件的前后哈希，以及临时配置清理。
- `rollback/`：编辑前的相关源码和测试备份，保留当时已有未提交修改；只覆盖本轮对应文件即可回退，不应重置整个仓库。新增测试和本文档可单独移除。

临时测试配置、请求体及终端环境快照已清理。另检查 90 个生成文件，没有发现残留真实测试密钥。扫描排除了静态技能、插件、兼容目录、媒体、符号链接及大于 10 MB 的文件；未删除用户历史或生产密钥配置。
