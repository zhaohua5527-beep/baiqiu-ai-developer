# 共享请求链修复与验收：速度尚未达标

2026-09-08。本轮已实施共享指令、供应商协议和预热复用修复，但真实天气任务仍超时，因此不通过“快速且稳定回复”的整体验收。

## 实际修改

业务代码共 7 个文件：

| 文件 | 修改内容 |
|---|---|
| `services/public-response-protocol.js`（新增） | 统一公开过程、阶段正文和最终答案指令。已核实阶段立即交付，下一步另发 action；不等待整个任务结束才补写阶段结果；普通回答使用正文。 |
| `main.js` | 聊天和直连请求引用共享协议；删除重复及永不启用的协议块；保留 maximum，去除“分析完整个任务才能开始动作”的歧义；预热避开正在执行的会话；记录供应商实际推理档位。 |
| `services/hms-project-runtime.js` | 项目请求也引用同一公开协议，消除旧的 answer/final 包装要求冲突。 |
| `services/hermes-config-service.js` | 原生路由同时核对端点和 API 风格；明确选择 Messages 时不被主机名强制改成另一协议。为官方 GLM-5.3/5.3-flash 增加已查证的协议配置，切换供应商后清除该配置。 |
| `services/baiqiu_acp_entry.py` | 应用自有兼容桥按端点、供应商、型号和 API 模式匹配 GLM 配置；修正原生运行时清除 reasoning_content 的问题；发送 thinking.enabled、clear_thinking=false、tool_stream=true，以及 maximum 对应的 max；增加实际 HTTP 请求的脱敏参数诊断。 |
| `services/hermes-acp-client.js` | 提交等待正在预热的同一会话，再核对已协商的 MCP 能力；复用现有会话并更新真实工具回调；迟到的预热不覆盖真实回调；增加 sessionReused 诊断。 |
| `services/hms-stream-failure.js` | 诊断通道只保留参数档位、布尔开关及字符数量，不保留推理文本、密钥或完整请求体。 |

测试新增 `test/request-speed-integration.test.js`；扩展 `test/test_tool_generation_bridge.py`；更新四份公开协议契约测试，使它们检查共享指令及相同语义，不再匹配已删除的重复英文段落。

本次没有修改渲染器、字体、颜色、布局、小剧场、工具执行器或安装目录中的 Hermes 源文件。原始代码、命令、工具结果仍在真实执行通道；没有把这些内容删掉或改成通用状态文案。没有新增模拟事件、自动降低推理或无工具聊天引擎，也没有通过延长超时冒充提速。

## 真实桌面结果

桌面入口：`白球AI-测试-11.lnk`，实际加载 `C:/Users/Lenovo/Desktop/白球AI-正式软件仓库/source-current`，版本 3.0.22。

默认供应商 zhipu，型号 glm-5.3-flash，推理设置 maximum。以下时间均从本次提交计起；首字采用现有可见 DOM 计时，不把连接状态、私有推理或小剧场计入公开进度。

| 请求 | 首条真实过程可见 | 正文首字可见 | 结果 |
|---|---:|---:|---|
| 重启后新会话“你好” | 无工具过程，未补造 | 16.457 秒 | 正常问候，完成 |
| 同会话再次“你好” | 无工具过程，未补造 | 20.242 秒 | 正常问候，完成 |
| 分别读取两份测试资料并交叉回复 | 15.219 秒 | 24.306 秒 | 两份库存分别 17、29，差值 12；49.827 秒完成 |
| 新会话查询内江本周天气 | 没有 | 没有 | 126.144 秒结束；收到模型数据，但触发现有两分钟无公开输出限制 |
| 长文生成中主动停止 | 无需等待正文 | 无 | 正常取消 |
| 取消后重新“你好” | 无工具过程，未补造 | 13.652 秒 | 正常问候，没有继续执行旧任务 |

文件任务的真实顺序：

1. 第一份真实读取完成后，在第 24.229 秒产生第一条 stage_result；普通正文随后出现。
2. 第二次工具调用在第 25.329 秒开始，晚于第一阶段回复。
3. 第二份读取完成后，在第 33.167 秒产生第二条 stage_result。
4. 模型随后产生真实 cross 结论及最终比较。原始工具执行仍保留在消息记录中。

该任务验证了“过程 → 阶段回复 → 过程 → 阶段回复”，不是前端模拟的固定步骤。stage_result 和 cross 均来自模型实际输出；没有为了让流程完整而补事件。

## 预热与剩余延迟

重启后的新会话提交等待已有预热约 1.032 秒，随后记录 sessionReused；同会话再次提交的复用耗时为 0 毫秒，未重建会话。文件任务等待预热约 0.443 秒后复用。

这证明本地复用修复生效，但并不证明端到端已变快：

- 首次问候：提交到 HTTP 请求约 2.364 秒；HTTP 请求到响应头约 10.548 秒；之后才生成公开正文。
- 热会话问候：提交到 HTTP 请求约 0.412 秒；HTTP 请求到响应头约 17.242 秒。热会话仍慢，不能归因于再次启动运行时。
- 天气请求：提交约 3.092 秒发出 HTTP 请求，约 8.000 秒收到响应头及模型数据；之后持续收流，结束前累计约 804 KB，但没有公开文字或工具调用。没有证据证明前端藏掉了已产生的公开过程。

网络首响应变化可能涉及供应商排队、计算或路由；当前数据不能把三者再分开。不能据此声称某个百分比的普遍提速，也不能把供应商等待统称为“已经到极限”。

## 供应商协议覆盖

已对现有 10 家官方路由执行本地契约检查：DeepSeek、OpenAI、Anthropic、Kimi、Qwen、智谱、混元、MiniMax、阶跃和小米。检查覆盖原生端点、代理端点隔离和 API 风格。这是协议路由检查，不是十家真实联网成功证明。

真实联网覆盖只有当前已有凭据的两条路线：

- 官方 GLM：实际请求包含 `reasoning_effort=max`、`thinking.type=enabled`、`thinking.clear_thinking=false`、`tool_stream=true`，仍保留 15 个工具。在独立新会话读取两份文件时，首过程 13.125 秒，首个阶段结果 22.614 秒，最终正文首字 49.107 秒，51.698 秒完成。
- 已配置代理模型 gpt-5.6-terra：独立临时运行时中，问候复核首字 9.832 秒；文件任务首过程 3.547 秒，两个阶段结果分别 9.532、16.415 秒，最终正文首字 31.867 秒，32.640 秒完成。没有向代理附加 GLM 专用参数。该路线现有能力记录没有证明原生 maximum 参数可用，仍保留最大推理提示，不声称证明了其内部推理强度。

代理测试早期脚本只记录 answerDeltas，漏了普通 visibleDelta，因此误判问候无正文。保存的真实 nativeText 证明问候成功；脚本修正后真实重测通过。这个误判不属于产品或供应商故障。

GLM 实际工具续轮的 reasoning_content 回传字符数为 0 → 934 → 1080 → 2415。另对原历史中五条真实消息使用安装版本的原生消毒函数及本次兼容桥复核：1397、425、1182、696、3610 个字符均逐字保留，工具调用数据不变。审计只输出数量与相等性，不公开私有推理文本。

GLM 专用配置目前仅针对文档已查证且本轮实际验证的 GLM-5.3/5.3-flash 官方标准端点；其他型号、Coding Plan、其他区域端点和没有密钥的供应商不能视为通过。其既有原生适配继续保留，未知接口不猜参数。

## 本地检查与数据保护

- 主要回归：87/87 JavaScript 测试通过，13/13 Python 测试通过。
- 共享协议和项目扩展检查：37 通过，2 个旧渲染断言失败。
- 额外旧工具协议检查：4 通过，3 个旧兜底断言失败。
- 这 5 个失败均用修改前的 main.js 基线复现，涉及旧日志裁剪或已禁用的工具结果兜底；本轮没有修改相关渲染/兜底代码，也没有为通过测试而恢复用户已否定的展示行为。
- 原有 316 条消息的角色、正文和创建时间逐条哈希比对：0 条变化。
- 默认供应商、型号、maximum 及现有凭据保持；恢复原先选中的会话。
- 完整 settings 哈希因运行期间状态更新发生变化，因此不声称整个配置对象逐字未变；已单独验证模型选择和凭据不变。应用还正常更新了检查/应用时间戳。
- 临时供应商测试配置和 `.env` 已移除；测试目录仅保留审计资料及合成测试会话。没有删除用户历史。

回滚备份、逐文件差异和原始计时位于：

`C:/Users/Lenovo/Desktop/白球AI-客户端/work/request-speed-implementation-20260908/`

主要证据：`implementation.diff`、`desktop-result.json`、`dom-timings.json`、`weather-wire-timings.json`、`official-protocol-result.json`、`echo-compat-check.json`、`proxy-result.json`、`proxy-recheck-result.json`、`final-audit.json` 及各测试日志。

## 结论与交付状态

可以测试本次修复版，但不能当作“速度问题已经根治”的版本发布。本地预热复用、官方 GLM 回传协议和交叉阶段回复已有真实证据；天气任务仍未通过速度与结果验收。

后续若继续治理端到端延迟，必须围绕同一请求的供应商排队/首响应和工具前持续生成行为做对照，并使用首过程、首字、正确性和重复样本共同验收。不能再用本地已复用、接口返回 200 或某次快样本代替整体通过结论。
