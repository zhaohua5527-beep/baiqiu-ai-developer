# 执行通道串入回答的修复记录

2026-09-08。已修复普通任务、项目任务以及执行记录重建时的通道状态混用，恢复两条已确认污染的天气回复。桌面首个内容超时仍能复现，不能将本次修复解释为所有供应商接入已经稳定。

## 原因与证据

受影响会话为 `local-e16aff71-29b0-464c-90be-293aef86f73a`，黑球会话为 `24352d2d-bc9e-4d0c-91fc-e53810a60ca3`。两个月天气对比对应原始数据库第 690—694 条记录。第 692 条的截图英文来自 reasoning 字段；实际工具调用和返回也确实存在。

原来的两个主进程入口先把不同 ACP 通道转成无来源字符串，再送入同一个 `HmsMessageStreamDemux`。进度封包中的阶段编号触发兼容解析状态 `pendingAnswerSegmentId`，随后到达的另一个通道继承这个状态，被当作 `answer_delta`。污染在主进程已经产生，随后进入保存的 `answerSegments`，历史页面据此恢复了错误正文。

使用该真实任务的已保存原始记录离线回放，按 1、7、4096 字符三种方式重新分块：旧路径均能复现截图内容进入回答；修复路径均得到 1344 字符公开回答，截图英文不再进入回答。分块方式是回归验证，不代表恢复了当时每个网络数据包。

## 文件与行为

- `services/hms-progress.js`：新增 `HmsUpdateStreamDemux`，分别维护 message 与 thought 通道的缓冲区、回答边界和进度解析状态；thought 通道只按明确封包识别公开产物，不能因阶段编号推定其后文字属于回答。自动生成的回答编号在两个通道之间保持唯一。`HmsProgressMapper` 的独立重建入口使用同一套分流实现。
- `main.js`：普通任务和项目任务均传递完整 update 对象，保留来源通道；移除已不使用的文本抽取别名。
- `test/hms-channel-isolation.test.js`：覆盖进度后的通道切换、未闭合回答、半截协议、供应商 reasoning 字段别名、公开阶段结果、自动编号、两阶段工具代码与返回内容、保存及回放。
- `test/hms-progress.test.js`、`test/black-ball-stream-contract.test.js`：修正把不同通道共用字符串解析状态视为正确行为的旧断言。

代码、命令、JSON 和工具返回继续通过实际工具事件进入执行过程；模型在公开答复中主动提供的代码块也继续保留。字体、小剧场、页面布局、模型设置及工具执行逻辑均未改动。此次修复不通过定时器或推测生成阶段事件。

## 两条历史记录的修复

任务脚本先用旧路径重现保存的回答，要求除空白外完全一致，再从同一批原始公开 message 记录重新解析。原记录备份后，替换污染的回答文本，并为修订的回答事件生成新的 eventId、记录 replacesEventId 和原始记录编号。

| 用户消息 ID | 修复前回答字符 | 修复后回答字符 | 原始 assistant 记录 |
| --- | ---: | ---: | --- |
| 50a5e25f-0162-4864-829f-d916b3e6e919 | 2262 | 847 | 678、680、682、684 |
| 26feeb6e-fb58-467a-9a2b-db188d94a9ab | 9228 | 1344 | 690、692、694 |

两条记录的 executionLog、toolCalls、structuredEvents 均做了修复前后相等校验。桌面重新读取后，正文正常，执行区分别保留 9 行和 6 行工具活动，原命令仍在执行区。没有批量改写其他历史记录。

原消息备份：`D:/白球AI/data/user-data-dev/backup/channel-repair-20260908/messages-before-1788860935296.json`。原始黑球数据库只读访问，未修改。

## 实际验证

- 核心回归 83/83 通过，包含真实代码保留、阶段折叠、历史回放及快照合并。语法和正常 Git 换行配置下的 diff 检查通过。
- 扩展回归 44 项中 35 通过、9 失败；相同 9 项在本轮修改前文件的隔离对照中同样失败，包括旧测试缺少 `persistedReplay` 注入、失效源码范围断言及此前已有的工具收尾断言。本轮未宣称全库测试通过。
- 桌面当前配置为官方智谱 `glm-5.3-flash`。普通 OK 请求首字 8.533 秒，显示 8.550 秒，完成 9.682 秒。
- 桌面两步文件任务及一次重试均在首个公开事件产生前触发 30 秒首个内容超时，总时长分别 31.733 秒、31.643 秒。桌面本轮两阶段真实任务验收未通过。
- 独立黑球真实调用保留当前运行时模型配置，读取两份随机内容文件。最终核验运行实际调用两次工具，按工具生成、调用、返回、阶段结果、再次生成、调用、返回、阶段结果顺序完成，两段公开回答均与文件内容一致，保存回放一致。实际工具返回类型为 `verification_result`。共观察到 515 个 thought 分块，合并后可检查的未标记长文本片段没有进入回答，公开答复共 156 字符。总时长 41.219 秒。
- 独立调用用于验证接收和分流，采用测试总时限 90 秒，未使用完整桌面提示或桌面的 30 秒首个内容时限，不能据此宣称桌面超时已解决。

## 兼容与运行位置

原有单通道字符串解析接口仍可用；工具事件协议、供应商接入参数和推理档位未变。公开封包继续从其实际通道解析，半截协议不能跨通道拼接。截图原始记录中另有一个无效 JSON 进度封包，本轮没有凭猜测补出对应公开事件。

已重新启动桌面“白球AI-测试-11”所指向的 `C:/Users/Lenovo/Desktop/白球AI-正式软件仓库/source-current`，版本仍为 3.0.22。修复前源码、差异、回归输出和实测证据位于 `C:/Users/Lenovo/Desktop/白球AI-客户端/work/channel-repair-20260908`。未创建分支或提交。

两个独立调用的临时运行时目录保留原始工具测试记录，均已移除复制的 `.env` 和 `config.yaml`。目录位置记录在各自测试报告中。
